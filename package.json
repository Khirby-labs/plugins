{
  "name": "@khirby/plugins",
  "private": true,
  "packageManager": "pnpm@11.0.9",
  "engines": {
    "node": ">=22",
    "pnpm": ">=11"
  },
  "scripts": {
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "echo \"Run tests from the Khirby CRM monorepo (API Jest roots include these packages).\""
  },
  "devDependencies": {
    "@khirby/plugin-host": "^1.0.0",
    "@khirby/plugin-sdk": "^1.0.0",
    "typescript": "^5.5.0"
  }
}
